import React from 'react'

const useUpdateFollowersAndFollowing = (setFollowing, setFollowers) => {
    // GOAL: delete the target user from either the current user's followers in real time

    return null;
}

export default useUpdateFollowersAndFollowing